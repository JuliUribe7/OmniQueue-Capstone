# Copy this file to .env and fill in your values
# .env is gitignored — never commit it

# Backend API URL — use teammate's network IP while on the same network
EXPO_PUBLIC_API_URL=https://team3.noblesolutionsenterprises.com
